<Window x:Class="House.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:House"
        mc:Ignorable="d"
        Title="ЖКХ" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{StaticResource Sec_BackBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="50" Height="50">
                    <Image Source="/Image/logo.png" Stretch="Uniform"/>
                </Border>
                <TextBlock Grid.Column="1" Text="ЖКХ" Style="{StaticResource HeaderText}" VerticalAlignment="Center" Margin="15,0,0,0"/>
                <Button Grid.Column="1" x:Name="LogoutButton" Content="Выход" Style ="{StaticResource NavButton}" Click="LogoutButton_Click" Margin="659,10,11,10"/>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="{StaticResource Sec_BackBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="Btn_Back" Content="Назад" Style="{StaticResource NavButton}" Click="Btn_Back_Click"/>
            </StackPanel>
        </Border>
        <Frame x:Name="MainFrame"
               Grid.Row="1"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
