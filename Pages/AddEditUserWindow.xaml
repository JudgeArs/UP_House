<Window x:Class="House.Pages.AddEditUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:House.Pages"
        mc:Ignorable="d"
        Title="AddEditUserWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Имя -->
        <TextBlock Grid.Row="0" Text="Имя:" Margin="0,5"/>
        <TextBox Grid.Row="1" x:Name="tbName" Margin="0,0,0,10"/>

        <!-- Логин -->
        <TextBlock Grid.Row="2" Text="Логин:" Margin="0,5"/>
        <TextBox Grid.Row="3" x:Name="tbLogin" Margin="0,0,0,10"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="4" Text="Пароль:" Margin="0,5"/>
        <PasswordBox Grid.Row="5" x:Name="pbPassword" Margin="0,0,0,10"/>

        <!-- Роль -->
        <TextBlock Grid.Row="6" Text="Роль:" Margin="0,5"/>
        <ComboBox Grid.Row="7" x:Name="cbRole" DisplayMemberPath="Role" Margin="0,0,0,20"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnSave" Content="Сохранить" 
                    Click="btnSave_Click" 
                    Background="{StaticResource AttentBrush}"
                    Foreground="White"
                    Padding="15,5" 
                    Margin="0,0,10,0"/>
            <Button x:Name="btnCancel" Content="Отмена" 
                    Click="btnCancel_Click"
                    Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>
