<Page x:Class="House.Pages.HouseStockPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:House.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HouseStockPage">

    <Grid Background="{StaticResource Prim_BackBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и кнопки -->
        <Border Grid.Row="0"
                Background="{StaticResource Sec_BackBrush}"
                Padding="10"
                Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" 
                        VerticalAlignment="Center">
                <TextBlock Text="Жилищный фонд" 
                           Style="{StaticResource HeaderText}"
                           Margin="0,0,20,0"/>
            </StackPanel>
        </Border>

        <!-- Список жилищного фонда -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Padding="10">
            <ListBox x:Name="HousingListBox"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="8"
                                Padding="15"
                                Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Адрес -->
                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="{Binding Address}"
                                           Style="{StaticResource HeaderText}"
                                           FontSize="16"
                                           Margin="0,0,0,5"/>


                                <!-- Детали -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                                    <TextBlock Text="{Binding Path=Start_the_managment, StringFormat='Начало управления: {0:dd.MM.yyyy}'}"
                                               Style="{StaticResource NormalText}"
                                               Margin="0,2,0,0"/>

                                    <TextBlock Text="{Binding Path=Floor, StringFormat='Этаж: {0}'}"
                                               Style="{StaticResource NormalText}"
                                               Margin="0,2,0,0"/>

                                    <TextBlock>
                                        <Run Text="Квартира:"/>
                                        <Run Text="{Binding Flat, TargetNullValue=' -'}"/>
                                    </TextBlock>

                                    <TextBlock Text="{Binding Path=Year, StringFormat='Год постройки: {0}'}"
                                               Style="{StaticResource NormalText}"
                                               Margin="0,2,0,0"/>

                                    <TextBlock Text="{Binding Path=Area, StringFormat='Площадь: {0} м²'}"
                                               Style="{StaticResource NormalText}"
                                               Margin="0,2,0,0"/>
                                </StackPanel>

                                <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                                        CornerRadius="4"
                                        Padding="5"
                                        Margin="0,10,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Заявок: "
                                                   Style="{StaticResource NormalText}"/>
                                        <TextBlock Text="{Binding Applications.Count}"
                                                   Style="{StaticResource NormalText}"
                                                   FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </ScrollViewer>

        <!-- Сообщение при отсутствии данных -->
        <TextBlock x:Name="NoDataTextBlock"
                   Grid.Row="1"
                   Text="Нет данных о жилищном фонде"
                   Style="{StaticResource HeaderText}"
                   Foreground="Gray"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
